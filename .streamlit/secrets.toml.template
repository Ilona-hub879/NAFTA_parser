# ── Streamlit Secrets template ────────────────────────────────────────────
# Copy this file to .streamlit/secrets.toml (local dev)
# OR paste the values into Streamlit Cloud → App Settings → Secrets
#
# HOW TO GET THE KEY:
#   1. console.cloud.google.com → Project: gen-lang-client-0545683338
#   2. APIs & Services → Credentials → Create Credentials → API key
#   3. API restrictions → restrict to: "Places API (New)"
#   4. Paste the key below

GOOGLE_PLACES_API_KEY = "YOUR_KEY_HERE"
